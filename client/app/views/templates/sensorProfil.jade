include ./mixins/deviceHeader.jade

.sensor
	+deviceHeader(sensor)		
	.row
		.col.s2
			.row
				.col.s1
					em Driver:
				.col.s1
					a(href="/drivers/#{sensor.driver.id}")= sensor.driver.name
			.row
				.col.s1
					em.tooltipped(data-position="left", data-delay="50", data-tooltip="ID used by the Driver to identify the device") Custom ID:
				.col.s1= sensor.customId